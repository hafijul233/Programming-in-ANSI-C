#include <stdio.h>

int main()
{

    float divisor, divider, result;

    printf("Enter Values of Divisor and Divider: ");
    scanf("%f %f",&divisor, &divider);
        result = divisor / divider;

        printf("%f %c %f = %f\n",divisor, 246, divider, result);

    return 0;
}
